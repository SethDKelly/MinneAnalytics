DATABASE_URL="file:./prisma/dev.db"
UPLOAD_DIR="./uploads"

# Used in submission confirmation and approval email stubs (optional in local dev)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Production notes (not wired in this demo):
# - Use PostgreSQL: DATABASE_URL="postgresql://..."
# - Store decks on S3 (or compatible object storage) instead of UPLOAD_DIR
